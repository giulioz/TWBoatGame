import { UserstatusColorPipe } from './userstatus-color.pipe';

describe('UserstatusColorPipe', () => {
  it('create an instance', () => {
    const pipe = new UserstatusColorPipe();
    expect(pipe).toBeTruthy();
  });
});
